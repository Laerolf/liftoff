services:
  db:
    image: postgres:16
    restart: always
    shm_size: 128mb
    environment:
      - POSTGRES_DB=liftoff
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=test
    ports:
      - 5432:5432
