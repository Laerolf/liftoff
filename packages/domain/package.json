{
  "name": "@liftoff/domain",
  "version": "0.0.1",
  "description": "The domain model of Liftoff.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/types/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "lint": "oxlint --fix",
    "format": "oxfmt",
    "test": "bun test --watch",
    "test:unit": "bun test --watch --filter test/unit",
    "test:ci": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "tsc --project tsconfig.build.json"
  },
  "dependencies": {
    "uuid": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
